---
interface Props {
  children: string;
  author?: string;
}

const { children, author } = Astro.props;
---

<blockquote class="my-8 pl-4 border-l-4 border-primary-500 dark:border-primary-400">
  <p class="text-xl italic text-gray-700 dark:text-gray-300">
    {children}
  </p>
  {author && (
    <footer class="mt-2 text-sm text-gray-500 dark:text-gray-400">
      — {author}
    </footer>
  )}
</blockquote> 